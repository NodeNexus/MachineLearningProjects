# Git Configuration Rule

Always ensure that any Git commits made in this workspace are authored using the user's email address: `vbalpandexe@gmail.com`. 
If you are initializing a new repository or doing operations that require configuring git, automatically run `git config user.email "vbalpandexe@gmail.com"`.
